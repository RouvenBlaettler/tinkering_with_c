#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main() {

    float x = 45;

    //x = sqrt(x);
    // x = pow(x, 3);
    //x = ceil(x);
    // x = round(x);
    //x = floor(x);
    //x = abs(x);
    //x = log(x);
    //x = sin(x);
    //x = cos(x);

    printf("%f\n", x);

    return 0;
}