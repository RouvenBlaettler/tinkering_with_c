#include <stdio.h>
#include <stdbool.h>

int main() { // && || !
    
    int isSunny = true;

    if(!isSunny){
        printf("It is cloudy");
    }
    else{
        printf("It's sunny");
    }

    return 0;

}